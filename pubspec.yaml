
name: product_quality_audit
description: Product Quality Audit - Dashboard Flutter Web + Firebase (demo)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  google_fonts: ^6.3.2
  http: ^1.2.0
  intl: ^0.20.2
  path_provider: ^2.1.5
  animated_text_kit: ^4.3.0
  fl_chart: ^0.69.0
  pdf: ^3.11.1
  printing: ^5.13.0
  flutter_lucide: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
